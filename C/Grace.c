#include <stdio.h>
/*
super se commentaire
*/
#define FT(x) int main(){FILE *fd = fopen(NAME, "w");fprintf(fd, RES, 10, 34, RES);}
#define NAME "Grace_kid.c"
#define RES "#include <stdio.h>%1$c/*%1$csuper se commentaire%1$c*/%1$c#define FT(x) int main(){FILE *fd = fopen(NAME, %2$cw%2$c);fprintf(fd, RES, 10, 34, RES);}%1$c#define NAME %2$cGrace_kid.c%2$c%1$c#define RES %2$c%3$s%2$c%1$c%1$cFT(xxxxxxxx)%1$c"

FT(xxxxxxxx)
